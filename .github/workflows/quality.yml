name: quality

on:
  push:
    branches: [main]
  pull_request:

jobs:
  repository:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Repository Doctor
        run: ./scripts/repository_doctor.sh
