# Release 0.8.3

## Fixed

- corrected Kotlin string literals in the shared authentication screen;
- replaced the complete shared dashboard render function;
- added source validation for the affected Kotlin functions;
- restored compilation of the shared Android core module.
